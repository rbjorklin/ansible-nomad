---
# vim: set softtabstop=2 tabstop=2 shiftwidth=2 expandtab autoindent smartindent syntax=yaml:

- hosts: localhost
  remote_user: root
  become: yes
  become_user: root
  roles:
    - ansible-nomad
